import React from 'react'

//TODO: determine what issuer UI should be (check trello board)

const issuerSubmissions = () => {
  return (
    <div>
      <h1>render submissions for issuer</h1>
    </div>
  )
}

export default issuerSubmissions
